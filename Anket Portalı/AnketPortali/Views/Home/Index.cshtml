@{
    ViewData["Title"] = "Home Page";
}
@model IEnumerable<Survey>
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger text-center" role="alert">
        @TempData["ErrorMessage"]
    </div>
}
<div class="container mt-4">
    <h2 class="mb-4 text-center">Aktif Anketler</h2>

    <table class="table table-striped table-bordered shadow-sm">
        <thead class="thead-dark">
            <tr>
                <th>Başlık</th>
                <th>Açıklama</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var survey in Model)
            {
                <tr>
                    <td>@survey.Title</td>
                    <td>@survey.Description</td>
                    <td class="text-center">
                        <form asp-action="TakeSurvey" asp-controller="Survey" method="post">
                            <input type="hidden" name="surveyId" value="@survey.Id" />
                            <button type="submit" class="btn btn-success btn-sm rounded-pill shadow-sm">Başvur</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    .table th, .table td {
        text-align: center;
        vertical-align: middle;
        padding: 15px;
    }

    .table {
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: #f2f2f2;
    }

    .thead-dark th {
        background-color: #007bff;
        color: white;
        font-weight: bold;
    }

    .btn {
        font-size: 1rem;
        padding: 10px 20px;
        transition: background-color 0.3s, transform 0.2s;
    }

    .btn:hover {
        background-color: #0056b3;
        transform: scale(1.05);
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }

    .container {
        max-width: 1200px;
        margin: auto;
        padding: 20px;
    }
</style>
